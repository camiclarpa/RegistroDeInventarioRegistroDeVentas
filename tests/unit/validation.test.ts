// Test de validación simple
describe('Validación del sistema', () => {
  it('debe tener Jest funcionando', () => {
    expect(true).toBe(true);
  });

  it('debe poder sumar números', () => {
    expect(1 + 1).toBe(2);
  });
});
